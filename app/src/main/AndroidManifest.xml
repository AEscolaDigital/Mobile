<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.school">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_educ_tec"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_educ_tec"
        android:supportsRtl="true"
        android:theme="@style/Theme.School"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.LoginActivity">

        </activity>

        </activity>
        <activity android:name=".activities.SchoolRegistrationActivity" />

        <activity android:name=".activities.TeamsActivity">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>